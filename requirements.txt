# AI Kernel OS — Python dependencies
# Phase 1: Core runtime (no LangGraph yet)

# LLM providers
langchain-core>=0.3.0
langchain-anthropic>=0.3.0
langchain-openai>=0.2.0

# HTTP client (web_tool)
httpx>=0.27.0

# HTML parsing (web_tool)
beautifulsoup4>=4.12.0

# YAML (workspace spec)
pyyaml>=6.0.0

# TUI
textual>=0.80.0

# Environment
python-dotenv>=1.0.0

# Gmail read-only OAuth adapter
google-api-python-client>=2.120.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# ──────────────────────────────────
# Phase 2 (uncomment when needed)
# ──────────────────────────────────
# langgraph>=0.3.0
# langgraph-checkpoint-sqlite>=2.0.0

# ──────────────────────────────────
# Phase 3 (uncomment when needed)
# ──────────────────────────────────
# sentence-transformers>=3.0.0
# faiss-cpu>=1.8.0
# playwright>=1.50.0
